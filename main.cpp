#include <iostream>
#include "Ingredient.hpp"
#include "Gateau.hpp"
#include "Chou.hpp"
#include "Tarte.hpp"
#include "Patisserie.hpp"

int main2() {
/*
    // Creation des ingredients
    Ingredient farine("Farine", 250);
    Ingredient sucre("Sucre", 100);
    Ingredient lait("Lait", 500);
    Ingredient oeufs("Oeufs", 4);
    Ingredient beurre("Beurre", 150);
    Ingredient chocolat("Chocolat", 200);
    Ingredient cremeFouettee("Creme fouettee", 200);

    // Creation d'un gateau
    Gateau gateauAuChocolat("Gateau au chocolat");
    gateauAuChocolat.ajouterIngredient(farine);
    gateauAuChocolat.ajouterIngredient(sucre);
    gateauAuChocolat.ajouterIngredient(lait);
    gateauAuChocolat.ajouterIngredient(oeufs);
    gateauAuChocolat.ajouterIngredient(beurre);
    gateauAuChocolat.ajouterIngredient(chocolat);

    // Affichage des ingredients du gateau
    gateauAuChocolat.afficherIngredients();

    // Creation d'un chou
    Chou chou("Creme patissiere", true, true, false);

    // Affichage des ingredients du chou
    chou.afficherIngredients();

    // Creation d'une tarte
    Tarte tarte("Pomme", true, false, true);

    // Affichage des ingredients de la tarte
    tarte.afficherIngredients();

    // Creation d'une patisserie
    Patisserie patisserie;

    // Ajout des gateaux, choux et tartes au stock de la patisserie
    patisserie.ajouter(&gateauAuChocolat);
    patisserie.ajouter(&chou);
    patisserie.ajouter(&tarte);

    // Affichage du stock de la patisserie
    patisserie.afficher();

    // Achat d'un chou
    bool achatReussi = patisserie.acheter(&chou);
    if (achatReussi) {
        std::cout << "Achat du chou reussi !" << std::endl;
    } else {
        std::cout << "Impossible d'acheter le chou, il n'est plus en stock." << std::endl;
    }

    // Achat d'un gateau
    achatReussi = patisserie.acheter(&gateauAuChocolat);
    if (achatReussi) {
        std::cout << "Achat du gateau reussi !" << std::endl;
    } else {
        std::cout << "Impossible d'acheter le gateau, il n'est plus en stock." << std::endl;
    }

    // Affichage du stock de la patisserie apres les achats
    patisserie.afficher();

    // Retrait d'un gateau du stock de la patisserie
    patisserie.retirer(&tarte);

    // Affichage du stock de la patisserie apres le retrait
    patisserie.afficher();*/


    return 0;
}
